import { RequestHandler } from 'express';

export const postRetrieveUser: RequestHandler = async (req, res, _next) => {};

export const postRetrieveOrders: RequestHandler = async (req, res, _next) => {};

export const postRetrievePersonalInfo: RequestHandler = async (req, res, _next) => {};

export const postRetrieveCredentials: RequestHandler = async (req, res, _next) => {};
